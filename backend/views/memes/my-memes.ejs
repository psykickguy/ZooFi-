<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>My Memes | ZooFi</title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1>My Memes</h1>

    <% if (memes.length === 0) { %>
    <p>You haven’t minted or bought any memes yet.</p>
    <% } else { %>
    <div class="meme-grid">
      <% memes.forEach(meme => { %>
      <div class="meme-card">
        <img src="<%= meme.imageUrl %>" alt="<%= meme.title %>" width="200" />
        <h3><%= meme.title %></h3>
        <p><%= meme.description %></p>
        <p><strong>Category:</strong> <%= meme.category %></p>
        <p><strong>Level:</strong> <%= meme.memeLevel %></p>
        <p>
          <strong>Minted At:</strong> <%= new Date(meme.mintedAt).toDateString()
          %>
        </p>

        <div class="meme-actions">
          <a href="/memes/<%= meme._id %>">View</a>
          <a href="/memes/<%= meme._id %>/edit">Edit</a>
          <a href="/memes/<%= meme._id %>/sell">Sell</a>
          <a href="/memes/<%= meme._id %>/transfer">Transfer</a>
        </div>
      </div>
      <% }); %>
    </div>
    <% } %>
  </body>
</html>
